var todos = ['item 1', 'item 2', 'item 3']; 

// It should have a way to delete a todo

todos.splice(0, 1); 

// (0 = position where to start deleting things in the array, 

// 1 = how many item to delete in the array)

// what was removed = [ 'item1 update' ]

// new array = [ 'item 2', 'item 3', 'item 4' ]
